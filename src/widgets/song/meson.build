moduledir = get_option('prefix') / get_option('datadir') / meson.project_name() / 'nocturne' / 'widgets' / 'song'

widgets = [
  '__init__.py',
  'row.py',
  'queue.py',
  'small_row.py',
]

install_data(widgets, install_dir: moduledir)