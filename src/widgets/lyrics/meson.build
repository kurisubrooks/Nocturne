moduledir = get_option('prefix') / get_option('datadir') / meson.project_name() / 'nocturne' / 'widgets' / 'lyrics'

widgets = [
  '__init__.py',
  'dialog.py',
  'helpers.py',
]

install_data(widgets, install_dir: moduledir)