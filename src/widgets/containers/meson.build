moduledir = get_option('prefix') / get_option('datadir') / meson.project_name() / 'nocturne' / 'widgets' / 'containers'

widgets = [
  '__init__.py',
  'carousel.py',
  'wrapbox.py'
]

install_data(widgets, install_dir: moduledir)