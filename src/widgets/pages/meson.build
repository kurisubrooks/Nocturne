moduledir = get_option('prefix') / get_option('datadir') / meson.project_name() / 'nocturne' / 'widgets' / 'pages'

widgets = [
  '__init__.py',
  'home.py',
  'sidebar.py',
  'login.py',
]

install_data(widgets, install_dir: moduledir)